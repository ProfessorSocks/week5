public class Encapsulation {
    /*
     * access modifers
     *  - public
     *      - accessable everywhere
     *  - private
     *      - only acessable in class
     *  - protected
     *      - only acessable in class and subclasses and package
     *  - No modifier
     *      - same as pprotected but not accessable in subclass in a diff package
     * 
     */
}
